/**
 * Reads the endpoint configuration vr-plugin.connection.config and returns the data.
 *
 * @param {number} maxConnections
 * @param {number} sessionTimeout
 * @param {number} connectTimeout
 */
//Auto generated script, cannot be modified !
System.getModule("com.vmware.library.vr").saveConfigurationSettings(maxConnections,connectTimeout,sessionTimeout) ;