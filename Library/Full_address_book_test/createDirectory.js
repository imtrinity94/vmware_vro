/**
 * Create a directory structure on the Automation Orchestrator server.
 *
 * @param {Path} directory
 */
//Auto generated script, cannot be modified !
System.getModule("com.vmware.basic").createDirectory(directory) ;