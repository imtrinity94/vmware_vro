/**
 * Reads the endpoint configuration srm-plugin.connection.config and returns the data.
 *
 * @param {number} maxConnections
 * @param {number} sessionTimeout
 * @param {number} connectTimeout
 */
//Auto generated script, cannot be modified !
System.getModule("com.vmware.library.srm").saveConfigurationSettings(maxConnections,connectTimeout,sessionTimeout) ;