/**
 * Log the input text to the console and the server log with level 'warn'
 *
 */
var text = "Error creating / updating property definitions. Rollback changes..."
System.warn(text);
Server.warn(text);
