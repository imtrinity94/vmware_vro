/**
 * Log the input text to the console and the server log with level 'warn'
 *
 */
var text = "Error registering Infoblox IPAM Endpoint type. Rollback changes..."
System.warn(text);
Server.warn(text);
