/**
 * Log the input text to the console log with level 'warn'
 *
 * @param {InfobloxIPAM:IpamConnection} currentConnection
 */
System.warn("Cannot establish connection with the IPAM host: " + currentConnection.hostName + "/" + currentConnection.apiType.name);
