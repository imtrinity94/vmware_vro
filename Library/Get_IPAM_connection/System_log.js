/**
 * Log the input text to the console log with level 'log'
 *
 * @param {InfobloxIPAM:IpamConnection} currentConnection
 */
System.log("Checking connectivity with the IPAM host: " + currentConnection.hostName + "/" + currentConnection.apiType.name + "...");