/**
 * Log the input text to the console log with level 'log'
 *
 * @param {InfobloxIPAM:IpamConnection} currentConnection
 */
System.log("Connection with the IPAM host was established successfully: " + currentConnection.hostName + "/" + currentConnection.apiType.name);
