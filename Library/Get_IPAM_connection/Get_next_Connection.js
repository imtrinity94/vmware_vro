/**
 * Get next Connection
 *
 * @param {Array/InfobloxIPAM:IpamConnection} ipamConnections
 * @param {number} currentConnectionIndex
 * @return {InfobloxIPAM:IpamConnection} currentConnection
 */
currentConnection = ipamConnections[currentConnectionIndex];
