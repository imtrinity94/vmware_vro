/**
 * Email Results
 *
 * @param {string} toAddress
 * @param {boolean} emailResults
 * @param {string} smtpHost
 * @param {number} smtpPort
 * @param {string} username
 * @param {SecureString} password
 * @param {string} fromName
 * @param {string} fromAddress
 * @param {Array/string} emailContent
 */
var message = new EmailMessage();
	
// Override default settings if and only if input parameter is set
if ( smtpHost != null && smtpHost.length > 0 ){
	message.smtpHost = smtpHost;
}
if ( smtpPort != null && smtpPort > 0 ){
	message.smtpPort = smtpPort;
}
if ( username !=null && username.length > 0){
	message.username = username;
}
if ( password != null && password.length > 0){
	message.password = password;
}
if ( fromName != null && fromName.length > 0){
	message.fromName = fromName;
}
if ( fromAddress != null && fromAddress.length > 0){
	message.fromAddress = fromAddress;
}
	
message.toAddress = toAddress;
message.subject = "Orphaned VMs";
	
var emailContentString = new String();
for (var j in emailContent) {
	emailContentString = emailContentString + "<p>" + emailContent[j] + "</p>";
}
emailContentString = emailContentString + "<p>" + "This email was generated by vCO" + "</p>";
	
message.addMimePart(emailContentString,"text/html");
	
System.log( "sending mail to host: " + message.smtpHost + ":" + message.smtpPort + " with user:" + message.username 
			+ ", from:" + message.fromAddress + ", to:" + message.toAddress );
	
message.sendMessage();