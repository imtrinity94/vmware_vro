/**
 * Generates read table record workflow
 *
 * @param {SQL:Table} table
 * @param {WorkflowCategory} category
 * @param {boolean} overwriteWorkflow
 * @return {Workflow} actionResult
 */
//Auto generated script, cannot be modified !
actionResult = System.getModule("com.vmware.library.sql").generateReadWorkflow(table,category,overwriteWorkflow) ;