/**
 * Log the input text to the console log with level 'log'
 *
 * @param {boolean} attrReconfigure
 * @param {string} attrReconfigureMAC
 * @return {string} attrMacAddress
 */
System.log("Initialization of MAC address is not required.");

if (attrReconfigure){
	attrMacAddress=attrReconfigureMAC;
	}
