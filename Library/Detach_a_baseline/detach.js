/**
 * detach
 *
 * @param {Array/VUM:Baseline} baselines
 * @param {Array/VUM:VIInventory} entities
 */
System.getModule("com.vmware.library.vmware_update_manager").detachBaselines(baselines,entities);