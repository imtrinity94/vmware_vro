/**
 * Register Local Site
 *
 * @param {string} host
 * @param {number} port
 * @param {string} path
 * @param {string} username
 * @param {SecureString} password
 */
SRMPluginConfig.registerLocalSites(host, port, path, username, password);