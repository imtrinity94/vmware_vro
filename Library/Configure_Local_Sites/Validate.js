/**
 * Validate
 *
 * @param {string} host
 * @param {number} port
 * @param {string} path
 * @param {string} username
 * @param {SecureString} password
 */
SRMPluginConfig.validateConnection(host, port, path, username, password);