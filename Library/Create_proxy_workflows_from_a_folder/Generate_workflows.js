/**
 * Generate workflows
 *
 * @param {VCO:RemoteWorkflowCategory} remoteFolder
 * @param {boolean} includeSubfolders
 * @param {boolean} synchronos
 */
VCOProxyWorkflowManager.createProxies(remoteFolder.id, includeSubfolders, synchronos);