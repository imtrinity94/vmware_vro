/**
 * Display Success Message
 *
 * @param {VC:VirtualMachine} VM
 * @param {PS:Volume} volumeCreated
 */
System.log("Volume " + volumeCreated.name + " has been successfully added to VM " + VM.name);