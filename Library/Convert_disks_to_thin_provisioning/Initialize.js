/**
 * Initialize
 *
 * @param {Array/VC:VirtualMachine} vmToConvert
 * @return {number} vmCount
 * @return {number} currentVM
 * @return {Array/VC:VirtualMachine} vmToConvert
 */
vmCount = vmToConvert.length;
currentVM = 0;