/**
 * Success Message
 *
 * @param {Array/VC:VirtualMachine} newVMs
 */
System.log("All the " + newVMs.length + " VMs are migrated successfully.");
System.log("Starting: Detach/unmount datastores and disconnect volumes.");
