/**
 * Set Snapshot
 *
 * @param {number} currentSnapshot
 * @param {Array/VC:VirtualMachineSnapshot} snapshots
 * @return {VC:VirtualMachineSnapshot} activeSnapshot
 */
// Set our activeVM object:
var activeSnapshot = snapshots[currentSnapshot];
