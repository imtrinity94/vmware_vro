/**
 * Initialize
 *
 * @param {Array/VC:VirtualMachineSnapshot} snapshots
 * @return {number} snapshotCount
 * @return {number} currentSnapshot
 * @return {Array/VC:VirtualMachineSnapshot} snapshots
 */
snapshotCount = snapshots.length;
currentSnapshot = 0;