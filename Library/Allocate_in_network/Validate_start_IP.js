/**
 * Validate start IP
 *
 */
// TODO: need to validate that specified IP address belongs to specified network:
// Error allocating IP address [x.x.x.x] because it doesn't belong to IP range [x.x.x.x-x.x.x.x], code: 1007